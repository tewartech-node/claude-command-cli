FROM node:20-alpine

WORKDIR /app

# Copy app files
COPY . /app/

# Install dependencies
RUN npm install 2>/dev/null || true

# Install Python for data access
RUN apk add --no-cache python3 py3-pip

# Run dashboard
EXPOSE 3000

CMD ["python3", "agent/dashboard/server.py"]
